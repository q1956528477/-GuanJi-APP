{
  "name": "guanji",
  "version": "1.11.0",
  "description": "观己 - 身心状态记录",
  "private": true,
  "author": "",
  "scripts": {
    "build:bridges": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/build.ps1",
    "test": "node test_liuyao.js",
    "test:page": "node test_app.js",
    "build": "npm run build:bridges",
    "cap": "cap sync android",
    "cap:add": "cap add android",
    "cap:sync": "cap sync android",
    "cap:open": "cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/app": "^6.0.3",
    "@capacitor/core": "^6.1.2",
    "@capacitor/filesystem": "^6.0.4",
    "@capacitor/local-notifications": "^6.1.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2",
    "esbuild": "^0.28.2",
    "jsdom": "^26.1.0"
  }
}
